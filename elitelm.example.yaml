default_backend: fake

backends:
  fake:
    kind: fake
    response_prefix: "EliteLM fake response: "

  # Uncomment and set `model` to a local GGUF file to use the llama.cpp CPU backend.
  # llamacpp_cpu:
  #   kind: llamacpp
  #   model: ./models/llama-3.2-3b.Q4_K_M.gguf
  #   n_threads: 8
  #   n_ctx: 4096
  #   n_batch: 512
  #   use_mmap: true
