[package]
name = "elitelm-backend-llamacpp"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[features]
# Gate real model inference tests so they only run when the user explicitly
# provides GGUF_TEST_MODEL in the environment.
integration = []

[dependencies]
anyhow.workspace = true
elitelm-core = { path = "../elitelm-core" }
elitelm-backend-llamacpp-sys = { path = "../elitelm-backend-llamacpp-sys" }
thiserror.workspace = true

[dev-dependencies]
tempfile.workspace = true
